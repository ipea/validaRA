---
title: "Relatório de validação de Documentos"
output: pdf_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)
library(ggthemes)
library(knitr)
library(ggplot2)
library(scales)
library(tidyr)
library(pander)
```
## Resultados

A base de dados foi validada utilizando-se o pacote ValidaRA. A tabela abaixo apresenta a quantidade
de dados válidos e inválidos por tipo de documento:


```{r tab2}
resultado$digitos <- resultado$primeiro_digito_invalido + resultado$segundo_digito_invalido
resultado <- resultado[c(-8,-9)]
kable(resultado, col.names = c("Tipo","Validos","Invalidos","Missing","Tam Invalido","Zeros","Iguais(1-9)","Digitos inválidos"))

```

\begingroup\footnotesize
Tam Invalido: o tamanho do dado informado era maior do que o necessário(excluindo-se os caracters especiais)

Zeros: Todos os dígitos eram iguais a zero.

Iguais(1-9): Todos os dígitos do documento eram iguais.

Digitos inválidos: o primeiro ou seguindo digito do documento não corresponde ao algoritmo de validação
\endgroup
